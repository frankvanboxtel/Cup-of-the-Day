{
  "name": "cup-of-the-day",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Node.js TypeScript project for Cup of the Day data.",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "node \"$npm_execpath\" run generate:all",
    "dev": "node \"$npm_execpath\" run generate:all",
    "generate": "tsx src/generate-results.ts",
    "generate:all": "node \"$npm_execpath\" run generate && node \"$npm_execpath\" run validate:results && node \"$npm_execpath\" run generate:aliases && node \"$npm_execpath\" run generate:html",
    "generate:aliases": "tsx src/generators/generate-alias-proposals.ts",
    "generate:html": "tsx src/generators/generate-html.ts",
    "validate:results": "tsx src/validation/validate-results.ts",
    "start": "node \"$npm_execpath\" run generate:all"
  },
  "dependencies": {
    "csv-parse": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}